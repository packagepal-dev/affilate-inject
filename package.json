{
  "name": "affilate-inject",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bring-your-own-key affiliate code injector (click-to-override). Use at your own risk.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
